namespace Websocket {

	export class MainEndPoint extends AventusSharp.WebSocket.EndPoint {
		
		/**
		 * Create a singleton
		 */
		public static override getInstance(): MainEndPoint {
			return Aventus.Instance.get(MainEndPoint);
		}
		
		protected override get path(): string {
			return "/ws";
		}
	}

}