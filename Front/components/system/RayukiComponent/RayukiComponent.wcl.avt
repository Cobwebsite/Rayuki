
namespace System {
	export abstract class RayukiComponent extends Aventus.WebComponent implements Aventus.DefaultComponent {
		protected override styleBefore(addStyle: (name: string) => void): void {
			super.styleBefore(addStyle);
			addStyle("@Rayuki");
		}
	}
}
