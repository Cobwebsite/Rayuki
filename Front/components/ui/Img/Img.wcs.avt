:host {
	/**
	* Set fill color and stroke color for svg img
	* @type color
	*/
	--_img-color: var(--img-color);
	/**
	* Set stroke color for svg img
	* @type color
	*/
	--_img-stroke-color: var(--img-stroke-color, var(--_img-color));
	/**
	* Set fill color for svg img
	* @type color
	*/
	--_img-fill-color: var(--img-fill-color, var(--_img-color));
	--_img-color-transition: var(--img-color-transition, none);
	--_img-stroke-width: var(--img-stroke-width, 1px);
}


:host {
	/* Your style here*/
	display: inline-block;
	font-size: 0;
	overflow: hidden;

	* {
		box-sizing: border-box;
	}

	img {
		opacity: 0;
		transition: filter 0.3s linear;
	}

	.svg {
		display: none;
		height: 100%;
		width: 100%;

		svg {
			height: 100%;
			width: 100%;
		}
	}

	mi-icon {
		display: none;
		font-size: inherit;
		height: 100%;
		width: 100%;
	}
}

:host([src$=".svg"]) {
	img {
		display: none;
	}

	.svg {
		display: flex;

		svg {
			fill: var(--_img-fill-color);
			stroke: var(--_img-stroke-color);
			stroke-width: var(--_img-stroke-width);
			transition: var(--_img-color-transition);
		}
	}

	mi-icon {
		display: none;
	}
}

:host([src^="mi-"]) {
	font-size: inherit;

	img {
		display: none;
	}

	.svg {
		display: none;
	}

	mi-icon {
		align-items: center;
		display: flex;
		justify-content: center;
	}
}

:host([display_bigger]) {
	img {
		cursor: pointer;
	}

	img:hover {
		filter: brightness(50%);
	}
}
