@import "../Button.wcs.avt";

:host {
	--_button-padding: var(--button-padding, 0);
	--_button-icon-font-size: var(--button-icon-font-size, inherit);
}

:host {
	aspect-ratio: 1 / 1;
	height: 36px;
	min-width: auto;
	border-radius: var(--border-radius-round);

	.hider {
		border-radius: var(--border-radius-round);
	}

	.content {
		.icon {
			--img-fill-color: var(--_button-color);
			height: 100%;
			padding: 0;
			font-size: var(--_button-icon-font-size);
		}
	}
}