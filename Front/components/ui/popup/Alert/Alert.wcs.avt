:host {
	.popup {
		.title {
			font-size: var(--font-size-lg);
			font-weight: bold;
			margin-bottom: 15px;
		}

		.action {
			display: flex;
			align-items: center;
			justify-content: center;
			gap:20px;
			margin-top: 30px;
		}
	}
}
